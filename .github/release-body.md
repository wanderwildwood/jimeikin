## Before you install

Coming from an older copy: uninstall it first. Android will not install this over it, and stops
with `INSTALL_FAILED_UPDATE_INCOMPATIBLE`. Uninstalling clears playlists, settings and anything
cached. Music files in your own storage are untouched.

Already on v1.0.4 or later: this installs over it and keeps everything.

---
